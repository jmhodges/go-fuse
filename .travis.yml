language: go

go_import_path: github.com/hanwen/go-fuse

sudo: required

before_install:
  - sudo apt-get install -qq pkg-config fuse
  - sudo modprobe fuse
  - sudo chmod 666 /dev/fuse
  - sudo chown root:$USER /etc/fuse.conf

install:
  - echo passing to ./all.bash

script:
  - ./all.bash
